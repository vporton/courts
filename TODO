- The top part of the UI is hidden when the vertical window size is small.

- Tabbed UI (split into multiple apps?)

- More roles.

- Automatic recurring (monthly, etc.) transactions.

- Cutting too long strings in controls.

- App initialization function.

- Intercourt transfer.
